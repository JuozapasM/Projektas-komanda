import type { AllTimeWinner, WinnerTeam } from "@/lib/types";

export function WinnersBoard({ lastGameWinners, allTimeWinners }: { lastGameWinners: WinnerTeam[]; allTimeWinners: AllTimeWinner[] }) {
  return <section className="winners-section"><div className="section-heading"><div><span className="mono eyebrow">Rezultatų galerija</span><h2>Nugalėtojai priekyje.</h2></div><span className="mono">TOP 3 / VISIEMS</span></div><div className="winners-grid"><div className="panel winners-panel"><div className="panel-heading"><div><h3>Paskutinio žaidimo nugalėtojai</h3><p>Visi komandos žaidėjai ir surinkti taškai.</p></div><span className="mono capacity">{lastGameWinners[0]?.gameDate}</span></div><table className="winner-table"><thead><tr><th>Vieta</th><th>Žaidėjai</th><th>Taškai</th></tr></thead><tbody>{lastGameWinners.map((winner) => <tr key={winner.place}><td><span className={`place place-${winner.place}`}>{winner.place}</span></td><td><strong>{winner.players.join(", ")}</strong></td><td className="points">{winner.points}</td></tr>)}</tbody></table></div><div className="panel winners-panel"><div className="panel-heading"><div><h3>Visų laikų nugalėtojai</h3><p>Daugiausiai taškų surinkę žaidėjai.</p></div><span className="mono capacity">TOP 3</span></div><table className="winner-table"><thead><tr><th>Vieta</th><th>Žaidėjas</th><th>Taškai / žaidimai</th></tr></thead><tbody>{allTimeWinners.map((winner, index) => <tr key={winner.name}><td><span className={`place place-${index + 1}`}>{index + 1}</span></td><td><strong>{winner.name}</strong></td><td className="points">{winner.points} <small>/ {winner.gamesPlayed} žaid.</small></td></tr>)}</tbody></table></div></div></section>;
}